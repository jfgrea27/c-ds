#include <stdio.h>
#include <stdlib.h>
#include "include/chashmap.h"

int main() {
    struct CHashMap *map = chashmap_create();

    // int k);ey = 42;
    // char *value = "Hello, World!";
    // chashmap_put(map, &key, value

    // char *retrieved = chashmap_get(map, &key);
    // printf("Retrieved value: %s\n", retrieved);

    // chashmap_delete(map, &key);
    // chashmap_free(map);

    return 0;
}